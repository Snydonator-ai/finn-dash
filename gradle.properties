org.gradle.jvmargs=-Xmx2048m
android.useAndroidX=false
android.nonTransitiveRClass=true
# Signing for the family build. This key only exists so that updates install
# over the top of an older version instead of being rejected as a different app.
FD_STORE_FILE=finndash-key.jks
FD_STORE_PASSWORD=finndash
FD_KEY_ALIAS=finndash
FD_KEY_PASSWORD=finndash
